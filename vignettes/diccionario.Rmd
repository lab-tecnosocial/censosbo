---
title: "Diccionario de datos — Censos de Bolivia (1976–2024)"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Diccionario de datos — Censos de Bolivia (1976–2024)}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", echo = FALSE)
library(censosbo)
library(DT)

opciones_dt <- list(
  pageLength  = 20,
  lengthMenu  = c(10, 20, 50, 100),
  autoWidth   = FALSE,
  language    = list(
    search      = "Buscar:",
    lengthMenu  = "Mostrar _MENU_ variables",
    info        = "Variables _START_ a _END_ de _TOTAL_",
    paginate    = list(previous = "Anterior", `next` = "Siguiente"),
    emptyTable  = "No hay variables que coincidan"
  ),
  columnDefs  = list(
    list(width = "130px", targets = 0),
    list(width = "40%",   targets = 1),
    list(width = "80px",  targets = 2),
    list(className = "dt-wrap", targets = 3)
  )
)

# Convierte una lista de data.frames de codebook en tabla DT
prep_tabla_hist <- function(meta_anio, tabla_nombre) {
  meta <- meta_anio[meta_anio$tabla == tabla_nombre, ]
  meta$categorias <- sapply(meta$valores_codigos, function(vc) {
    if (is.null(vc) || nrow(vc) == 0) return("")
    paste(paste0(vc$codigo, " = ", vc$etiqueta), collapse = " | ")
  })
  df <- meta[, c("variable", "etiqueta", "tipo", "categorias")]
  names(df) <- c("Variable", "Etiqueta", "Tipo", "Categorías")
  df
}

# Para el CPV-2024 (usa codebook_meta global)
prep_tabla_2024 <- function(tabla_nombre) {
  meta <- codebook_meta[codebook_meta$tabla == tabla_nombre, ]
  meta$categorias <- sapply(meta$valores_codigos, function(vc) {
    if (is.null(vc) || nrow(vc) == 0) return("")
    paste(paste0(vc$codigo, " = ", vc$etiqueta), collapse = " | ")
  })
  df <- meta[, c("variable", "etiqueta", "tipo", "categorias")]
  names(df) <- c("Variable", "Etiqueta", "Tipo", "Categorías")
  df
}

hacer_datatable <- function(df) {
  DT::datatable(
    df,
    rownames  = FALSE,
    filter    = "top",
    escape    = FALSE,
    options   = opciones_dt,
    class     = "cell-border stripe compact"
  )
}
```

Este artículo documenta todas las variables de los cinco censos de Bolivia disponibles en `censosbo`: **1976, 1992, 2001, 2012 y CPV-2024**. Las tablas son interactivas — puedes buscar por nombre de variable, descripción o categoría.

Para explorar el diccionario desde R:

```{r codebook-r, echo=TRUE, eval=FALSE}
# Buscar una variable en cualquier censo
codebook(buscar = "educacion", anio = 2012)
codebook_2024(buscar = "sexo")

# Ver los códigos de una variable
codebook_valores("p25_sexo")           # CPV-2024
codebook_valores("P24", anio = 2012)   # Censo 2012

# Ver todas las variables de un censo y tabla
codebook_1992(tabla = "persona")
```

La columna **Tipo** clasifica cada variable como:

- **`categorica`** — sus valores son códigos con significado. Incluye variables
  cuyos valores son números pero representan categorías (p. ej. `sexo` 1/2) y
  códigos de clasificación con nombre terminado en `cod` (departamento, municipio,
  ocupación, idioma…), aunque no todos sus códigos estén enumerados.
- **`numerica`** — conteos y medidas continuas (edad, número de habitaciones…).
- **`texto`** — texto libre.

Esta clasificación está disponible también en la columna `tipo` de los
`diccionario_variables.parquet` publicados.

---

# CPV-2024

## Persona (`get_personas_2024`)

Datos de cada persona empadronada: **`r nrow(codebook_meta[codebook_meta$tabla=="persona",])`** variables. La tabla trae una columna más, `i00`
(identificador de vivienda/hogar), que no es una variable del cuestionario sino
parte de la clave de unión — ver más abajo.

```{r tabla-2024-persona}
hacer_datatable(prep_tabla_2024("persona"))
```

---

## Vivienda (`get_viviendas_2024`)

Características de cada vivienda: **`r nrow(codebook_meta[codebook_meta$tabla=="vivienda",])`** variables, más `i00`.

```{r tabla-2024-vivienda}
hacer_datatable(prep_tabla_2024("vivienda"))
```

---

## Emigración (`get_emigracion_2024`)

Personas que emigraron al exterior en los últimos 5 años: **`r nrow(codebook_meta[codebook_meta$tabla=="emigracion",])`** variables.

```{r tabla-2024-emigracion}
hacer_datatable(prep_tabla_2024("emigracion"))
```

---

## Mortalidad (`get_mortalidad_2024`)

Fallecimientos en el hogar durante los últimos 12 meses: **`r nrow(codebook_meta[codebook_meta$tabla=="mortalidad",])`** variables.

```{r tabla-2024-mortalidad}
hacer_datatable(prep_tabla_2024("mortalidad"))
```

---

## Unidades censales: manzanos y comunidades (`get_unidades_2024`)

Universo de las 268.604 unidades censales del CPV-2024 —manzanos urbanos y
comunidades rurales— con su población y sus viviendas:
**`r nrow(codebook_meta[codebook_meta$tabla=="unidad",])`** variables.

```{r tabla-2024-unidad}
hacer_datatable(prep_tabla_2024("unidad"))
```

---

## Indicadores por manzano y comunidad (`get_fichas_2024`)

Ficha resumen del geoportal del INE para las 150.744 unidades que la tienen
publicada: **`r nrow(codebook_meta[codebook_meta$tabla=="ficha",])`** variables
(194 indicadores más las 5 columnas de identificación). Todas son **conteos** de
personas, viviendas u hogares; cada bloque temático trae su propio total, que es
el denominador correcto. Ver `vignette("manzanos-comunidades")`.

```{r tabla-2024-ficha}
hacer_datatable(prep_tabla_2024("ficha"))
```

---

## Clave geográfica (CPV-2024)

Todas las tablas del CPV-2024 comparten la **clave de hogar**:

| Variable | Descripción |
|----------|-------------|
| `idep`   | Código de departamento (01–09) |
| `iprov`  | Código de provincia |
| `imun`   | Código de municipio |
| `i00`    | Identificador de vivienda/hogar |

```{r clave-geo, echo=TRUE, eval=FALSE}
library(DBI)
con <- DBI::dbConnect(duckdb::duckdb())
duckdb::duckdb_register_arrow(con, "personas",  get_personas_2024(departamento = "07"))
duckdb::duckdb_register_arrow(con, "viviendas", get_viviendas_2024(departamento = "07"))
DBI::dbGetQuery(con, "
  SELECT COUNT(*) AS personas_en_join FROM personas p
  JOIN viviendas v ON p.idep=v.idep AND p.iprov=v.iprov
                  AND p.imun=v.imun AND p.i00=v.i00
")
DBI::dbDisconnect(con, shutdown = TRUE)
```

Las tablas `unidad` y `ficha` no usan esta clave: se identifican por su propia
columna `codigo` de unidad censal, y traen `idep`/`iprov`/`imun` desnormalizados
para poder agregar por territorio.

---

## Área urbana/rural y grupos de edad derivados (CPV-2024)

Algunas columnas de la tabla **persona** son **derivadas** por el INE (no son
preguntas del cuestionario). Todas están en el diccionario y se etiquetan con
`etiquetar_valores()`.

**Urbano/rural.** El mismo concepto usa dos nombres según la tabla:

| Tabla     | Columna  | Códigos                |
|-----------|----------|------------------------|
| persona   | `area`   | `1 = Urbana`, `2 = Rural` |
| vivienda  | `urbrur` | `1 = Urbana`, `2 = Rural` |

```{r area-r, echo=TRUE, eval=FALSE}
codebook_valores("area")     # persona
codebook_valores("urbrur")   # vivienda
```

**Grupos de edad.** La tabla persona trae varias agrupaciones de edad, además
de la edad exacta en años (`p26_edad`, numérica). Se distinguen por sus cortes:

| Variable    | Descripción                                   | Cortes |
|-------------|-----------------------------------------------|--------|
| `edad_qui`  | Edad quinquenal                               | 0–4, 5–9, …, 95–99, 100+ |
| `g_edad`    | Grandes grupos de edad (estándar internacional) | 0–14, 15–64, 65+ |
| `g_edad_bol`| Grandes grupos de edad (corte boliviano)      | 0–14, 15–59, 60+ |
| `gedadedu`  | Grupo de edad según asistencia educativa      | 0–3, 4–5, 6–11, 12–17, 18–24, 25–59, 60+ |

```{r gedad-r, echo=TRUE, eval=FALSE}
codebook_valores("g_edad")       # 0-14 / 15-64 / 65 y más
codebook_valores("g_edad_bol")   # 0-14 / 15-59 / 60 y más (Bolivia)
codebook_valores("edad_qui")     # quinquenios
codebook_valores("gedadedu")     # grupos de edad educativa
```

---

# Censo 2012

```{r n-2012, include=FALSE}
m12 <- codebook_historico_meta[["2012"]]
```

## Persona (`get_personas_2012`)

**`r nrow(m12[m12$tabla=="persona",])`** variables.

```{r tabla-2012-persona}
hacer_datatable(prep_tabla_hist(m12, "persona"))
```

---

## Vivienda (`get_viviendas_2012`)

**`r nrow(m12[m12$tabla=="vivienda",])`** variables.

```{r tabla-2012-vivienda}
hacer_datatable(prep_tabla_hist(m12, "vivienda"))
```

---

## Emigración (`get_emigracion_2012`)

**`r nrow(m12[m12$tabla=="emigracion",])`** variables.

```{r tabla-2012-emigracion}
hacer_datatable(prep_tabla_hist(m12, "emigracion"))
```

---

## Discapacidad (`get_discapacidad_2012`)

**`r nrow(m12[m12$tabla=="discapacidad",])`** variables.

```{r tabla-2012-discapacidad}
hacer_datatable(prep_tabla_hist(m12, "discapacidad"))
```

---

# Censo 2001

```{r n-2001, include=FALSE}
m01 <- codebook_historico_meta[["2001"]]
```

## Persona (`get_personas_2001`)

**`r nrow(m01[m01$tabla=="persona",])`** variables.

```{r tabla-2001-persona}
hacer_datatable(prep_tabla_hist(m01, "persona"))
```

---

## Vivienda (`get_viviendas_2001`)

**`r nrow(m01[m01$tabla=="vivienda",])`** variables.

```{r tabla-2001-vivienda}
hacer_datatable(prep_tabla_hist(m01, "vivienda"))
```

---

# Censo 1992

```{r n-1992, include=FALSE}
m92 <- codebook_historico_meta[["1992"]]
```

## Persona (`get_personas_1992`)

**`r nrow(m92[m92$tabla=="persona",])`** variables.

```{r tabla-1992-persona}
hacer_datatable(prep_tabla_hist(m92, "persona"))
```

---

## Vivienda (`get_viviendas_1992`)

**`r nrow(m92[m92$tabla=="vivienda",])`** variables.

```{r tabla-1992-vivienda}
hacer_datatable(prep_tabla_hist(m92, "vivienda"))
```

---

## Mortalidad (`get_mortalidad_1992`)

**`r nrow(m92[m92$tabla=="mortalidad",])`** variables.

```{r tabla-1992-mortalidad}
hacer_datatable(prep_tabla_hist(m92, "mortalidad"))
```

---

# Censo 1976

```{r n-1976, include=FALSE}
m76 <- codebook_historico_meta[["1976"]]
```

## Población (`get_poblacion_1976`)

**`r nrow(m76[m76$tabla=="poblacion",])`** variables.

```{r tabla-1976-poblacion}
hacer_datatable(prep_tabla_hist(m76, "poblacion"))
```

---

## Vivienda (`get_viviendas_1976`)

**`r nrow(m76[m76$tabla=="vivienda",])`** variables.

```{r tabla-1976-vivienda}
hacer_datatable(prep_tabla_hist(m76, "vivienda"))
```

---

## Fuentes

> Instituto Nacional de Estadística (INE). Bolivia. Censos Nacionales de Población y Vivienda, 1976, 1992, 2001, 2012 y 2024. <https://anda.ine.gob.bo>
