% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fichas.R
\name{get_geo_comunidades}
\alias{get_geo_comunidades}
\title{Descarga las geometrías de las comunidades rurales del CPV-2024}
\usage{
get_geo_comunidades(
  departamento = NULL,
  provincia = NULL,
  municipio = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{departamento}{Nombre o código del departamento (e.g., `"Cochabamba"`
o `"03"`). `NULL` (defecto) devuelve todo el país.}

\item{provincia}{Nombre o código de la provincia. Opcional.}

\item{municipio}{Nombre o código del municipio. Opcional.}

\item{overwrite}{Lógico. Si `TRUE`, re-descarga aunque exista en caché.}

\item{verbose}{Lógico. Mostrar progreso.}
}
\value{
Un objeto `sf` con columnas `codigo`, `nombre`, `idep`, `iprov`,
  `imun` y `geometria` (EPSG:4326).
}
\description{
Descarga las geometrías de las comunidades rurales del CPV-2024
}
\details{
El INE publica la mayoría de las comunidades rurales como **puntos** (un
centro aproximado), no como polígonos. Para mapas de coropletas rurales no
hay superficie que rellenar: conviene usar [ggplot2::geom_sf()] con puntos
graduados por tamaño o color.
}
\examples{
\dontrun{
comunidades <- get_geo_comunidades(departamento = "Pando")
plot(comunidades["nombre"])
}
}
