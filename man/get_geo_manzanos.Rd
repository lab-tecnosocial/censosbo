% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fichas.R
\name{get_geo_manzanos}
\alias{get_geo_manzanos}
\title{Descarga las geometrías de los manzanos urbanos del CPV-2024}
\usage{
get_geo_manzanos(
  departamento = NULL,
  provincia = NULL,
  municipio = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{departamento}{Nombre o código del departamento (e.g., `"Cochabamba"`
o `"03"`). `NULL` (defecto) devuelve todo el país.}

\item{provincia}{Nombre o código de la provincia. Opcional.}

\item{municipio}{Nombre o código del municipio. Opcional.}

\item{overwrite}{Lógico. Si `TRUE`, re-descarga aunque exista en caché.}

\item{verbose}{Lógico. Mostrar progreso.}
}
\value{
Un objeto `sf` con columnas `codigo`, `nombre`, `idep`, `iprov`,
  `imun` y `geometria` (polígonos, EPSG:4326).
}
\description{
Descarga las geometrías de los manzanos urbanos del CPV-2024
}
\details{
Los archivos están partidos por departamento (0,3 a 6,7 MB cada uno). Sin el
argumento `departamento` se descargan los nueve, unos 25 MB en total; para
un mapa de un municipio conviene acotar.
}
\examples{
\dontrun{
manzanos <- get_geo_manzanos(municipio = "Sucre")
plot(manzanos["nombre"])
}
}
\seealso{
[mapa_man()] para dibujarlas, [get_geo_comunidades()] para el área
  rural.
}
