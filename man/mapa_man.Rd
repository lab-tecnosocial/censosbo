% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{mapa_man}
\alias{mapa_man}
\title{Visualiza una variable a nivel de manzano o comunidad}
\usage{
mapa_man(
  datos,
  variable,
  municipio,
  departamento = NULL,
  area = NULL,
  titulo = NULL,
  etiqueta_fill = NULL,
  paleta = NULL,
  na_color = "grey80",
  tamano_punto = 0.9
)
}
\arguments{
\item{datos}{Data.frame con una columna `codigo` de unidad censal y la
columna a visualizar. Típicamente sale de [get_fichas_2024()] o
[get_unidades_2024()].}

\item{variable}{Nombre (caracter) de la columna a visualizar.}

\item{municipio}{Nombre o código del municipio a mapear. Obligatorio: son
~268.000 unidades en todo el país y un mapa nacional no se lee.}

\item{departamento}{Departamento del municipio. Solo hace falta para
desambiguar nombres repetidos entre departamentos.}

\item{area}{Qué unidades incluir: `"urbano"`, `"rural"` o ambas (defecto).}

\item{titulo}{Título del mapa. Si `NULL`, usa el nombre de la variable.}

\item{etiqueta_fill}{Etiqueta de la leyenda. Si `NULL`, usa `variable`.}

\item{paleta}{Paleta de color. Por defecto `"Blues"` (continua) o `"Set3"`
(categórica).}

\item{na_color}{Color para unidades sin datos. Por defecto `"grey80"`.}

\item{tamano_punto}{Tamaño de los puntos de las comunidades rurales.}
}
\value{
Un objeto `ggplot` modificable con capas adicionales de ggplot2.
}
\description{
Genera un mapa de un municipio al máximo detalle disponible del CPV-2024:
cada manzano urbano como polígono y cada comunidad rural como punto.
}
\details{
Las geometrías se descargan al caché con [get_geo_manzanos()] y
[get_geo_comunidades()] la primera vez; después se reutilizan.

Las unidades del municipio que no estén en `datos` se dibujan con `na_color`.
Eso es lo normal al mapear [get_fichas_2024()]: el 47% de los manzanos no
tiene ficha por reserva estadística, y salen en gris.
}
\examples{
\dontrun{
library(dplyr)
agua <- get_fichas_2024(municipio = "Sucre", as = "tibble") |>
  mutate(pct_caneria = 100 * serv_agua_caneria / serv_agua_total)
mapa_man(agua, "pct_caneria", municipio = "Sucre",
         titulo = "\% viviendas con agua por cañería - Sucre (2024)")
}
}
\seealso{
[mapa_mun()] para el nivel municipal, [mapa_dep()] para el departamental.
}
