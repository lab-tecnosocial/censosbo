% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temas.R
\name{codebook_docs}
\alias{codebook_docs}
\title{Definición, universo y pregunta literal de una variable}
\usage{
codebook_docs(variable, tabla = NULL, campos = NULL, anio = 2024)
}
\arguments{
\item{variable}{Caracteres. Nombre(s) de variable.}

\item{tabla}{Caracteres. Desambigua cuando la variable existe en varias tablas.}

\item{campos}{Caracteres. Devuelve solo estas columnas de documentación
(e.g. `"definicion"`, `"pregunta_literal"`, `"regla_derivacion"`).}

\item{anio}{Entero. Censo o censos: `2024` (defecto), `2012`, `2001`. Acepta
varios, que es la forma de comparar cómo cambió una definición entre censos.}
}
\value{
Un data.frame con una fila por variable y año encontrados.
}
\description{
Accesor de [codebook_docs_meta]: los textos oficiales del INE sobre una
variable. Útil para resolver qué mide exactamente, a quién se le preguntó, o
cómo se construyó una variable derivada.
}
\examples{
# ¿Qué mide exactamente y a quién se le preguntó?
codebook_docs("p40_lee", campos = c("definicion", "universo_literal"))

# ¿Cómo construyó el INE esta variable derivada?
codebook_docs("nivel_edu", campos = "regla_derivacion")
}
\seealso{
[codebook()] para las etiquetas y categorías, y [codebook_valores()]
  para los códigos de una variable categórica.
}
