% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{mapa_dep}
\alias{mapa_dep}
\title{Visualiza una variable censal a nivel departamental}
\usage{
mapa_dep(
  datos,
  variable,
  titulo = NULL,
  etiqueta_fill = NULL,
  paleta = NULL,
  na_color = "grey80",
  mostrar_nombres = FALSE
)
}
\arguments{
\item{datos}{Data.frame con al menos las columnas `idep` y `variable`.
Típicamente el resultado de una agregación con dplyr.}

\item{variable}{Nombre (caracter) de la columna a visualizar.}

\item{titulo}{Título del mapa. Si `NULL`, usa el nombre de la variable.}

\item{etiqueta_fill}{Etiqueta de la leyenda. Si `NULL`, usa `variable`.}

\item{paleta}{Paleta de color. Por defecto `"Blues"` (continua) o `"Set3"`
(categórica).}

\item{na_color}{Color para departamentos sin datos. Por defecto `"grey80"`.}

\item{mostrar_nombres}{Si `TRUE`, agrega etiquetas con nombres de departamentos.}
}
\value{
Un objeto `ggplot` modificable con capas adicionales de ggplot2.
}
\description{
Genera un mapa coroplético de Bolivia a nivel de departamento.
}
\details{
Compatible con todos los censos (1976–2024): los 9 departamentos son estables.
Para el censo 1976, que usa la columna `dep` en lugar de `idep`, renómbrala:
`datos$idep <- datos$dep` (los códigos se normalizan a 2 dígitos
automáticamente, así que da igual que vengan como enteros).
}
\examples{
\dontrun{
library(dplyr)
pob <- get_personas_2024(as = "tibble") |>
  count(idep, name = "poblacion")
mapa_dep(pob, "poblacion", titulo = "Población por departamento (CPV-2024)")
}
}
