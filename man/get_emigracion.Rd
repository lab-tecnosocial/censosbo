% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_emigracion.R
\name{get_emigracion}
\alias{get_emigracion}
\title{Accede a los microdatos de emigración internacional del CPV-2024}
\usage{
get_emigracion(
  departamento = NULL,
  provincia = NULL,
  municipio = NULL,
  variables = NULL,
  as = c("arrow", "tibble", "duckdb"),
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{departamento}{Vector de caracteres. Código(s) de departamento
(`"01"`-`"09"`) o nombre(s) (e.g., `"La Paz"`, `"Santa Cruz"`).
Si `NULL`, incluye todos los departamentos (descarga ~500 MB).}

\item{provincia}{Vector de caracteres. Código(s) de provincia. Si `NULL`,
incluye todas.}

\item{municipio}{Vector de caracteres. Código(s) de municipio. Si `NULL`,
incluye todos.}

\item{variables}{Vector de caracteres. Nombres de columnas a seleccionar.
Si `NULL`, devuelve todas (118 columnas). Las columnas geográficas
(`idep`, `iprov`, `imun`, `i00`) siempre se incluyen.}

\item{as}{Formato de retorno: `"arrow"` (por defecto), `"tibble"` o
`"duckdb"` (tabla `"emigracion"`).}

\item{overwrite}{Lógico. Si `TRUE`, re-descarga aunque exista en caché.}

\item{verbose}{Lógico. Mostrar mensajes de progreso. Por defecto `TRUE`.}
}
\value{
Ver [get_personas()].
}
\description{
Descarga y/o carga desde caché los datos de emigración internacional del
Censo de Población y Vivienda 2024 de Bolivia.
}
\details{
Registra los miembros del hogar que emigraron al exterior en los últimos
5 años. Variables principales: sexo, año de salida, edad al salir y país
de destino (`pais_destino_cod`).
}
\examples{
\dontrun{
# Emigración por país de destino
library(dplyr)
get_emigracion() |>
  count(pais_destino_cod, sort = TRUE) |>
  collect()
}
}
